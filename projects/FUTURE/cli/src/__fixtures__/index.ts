import defaultPackageJson from './package.default'
import defaultTsConfig from './tsconfig.default'

export default {
  packageJson: {
    default: defaultPackageJson,
  },
  tsconfig: {
    default: defaultTsConfig,
  },
}