import defaultPackageJson from './npm-package.json'
import defaultTsConfig from './tsconfig.json'

export default {
  packageJson: {
    default: defaultPackageJson,
  },
  tsconfig: {
    default: defaultTsConfig,
  },
}