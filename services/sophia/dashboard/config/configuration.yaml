
# Configure a default setup of Home Assistant (frontend, api, etc)
default_config:

# Uncomment this if you are using SSL/TLS, running in Docker container, etc.
http:
  ssl_certificate: /tls/live/sophia.the-pegasus.net/fullchain.pem
  ssl_key: /tls/live/sophia.the-pegasus.net/privkey.pem
  base_url: !secret http_base_url

namecheapdns:
  host: !secret namecheap_dns_host
  domain: !secret namecheap_dns_domain
  password: !secret namecheap_dns_password

# Text to speech
tts:
  - platform: google_translate

group: !include groups.yaml
automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml
