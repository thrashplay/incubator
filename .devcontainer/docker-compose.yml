version: '3'
services:
  development:
    build: .
    entrypoint: /bin/sh -c "while sleep 1000; do :; done"
    volumes:
      - remote-workspace:/workspace
    ports:
    - "8081:8081"

  emulator:
    build: vnc:tmp
    privileged: true
    ports:
    - "5900:5900"

volumes:
  remote-workspace: